public class Define
{
    public enum eGameState : byte
    {
        Ready,
        Play,
        End,
    }

    public enum eSound : byte
    {
        Bgm,
        MaxCount
    }

    public enum eBlockType : byte
    {
        Pingpong,
        None
    }
}
